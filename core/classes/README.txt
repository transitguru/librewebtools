This direcory includes core classes which are not associated to any optional 
and are essential to loading the site. Any other module-specific classes will 
reside within its module directory and will be loaded after the core classes, 
to allow for potential inheritance of core classes.  
